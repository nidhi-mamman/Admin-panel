services:
  - type: web
    name: techcadd-django
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn techcadd_apis.wsgi:application
